<section class="section">
  <div class="container">
    <div class="columns">
      <%# if user_signed_in? %>
        <%#= render 'profile' %>
      <%= render 'follow-suggestions' %>
      <%# else %>
        <%#= render 'trends' %>
      <%# end %>

      <%= render 'feed' %>
      <%= render 'follow-suggestions' %>
    </div>
  </div>
</section>